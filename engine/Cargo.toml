[package]
name = "honeycomb-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.2", features = [ "process", "mount", "sched", "hostname", "fs" ] }

log = "0.4.29"

tracing = "0.1"
tracing-subscriber = "0.3"
