[package]
name = "honeycomb-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.2", features = [ "process", "mount", "sched", "hostname", "fs" ] }

[[bin]]
name = "honeycomb-runtime"
bench = false
test = false
